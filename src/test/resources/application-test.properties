spring.datasource.url=jdbc:postgresql://${CI_DB_HOST_PORT:localhost:12222}/imdb-test
spring.datasource.username=springtest
spring.datasource.password=springtest

# just to be more flexible while testing
spring.sql.init.mode=always

# path to the test dataset
spring.sql.init.data-locations=classpath:sql/data-test.sql